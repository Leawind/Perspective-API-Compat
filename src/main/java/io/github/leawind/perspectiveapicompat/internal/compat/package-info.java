/// Compatibility integrations.
///
/// Every target-mod dependency in this package is optional. Keep target-mod references isolated
/// behind a loaded-mod check so the base mod can load without any compatibility target installed.
package io.github.leawind.perspectiveapicompat.internal.compat;
